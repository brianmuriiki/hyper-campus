export default function Placeholder({ label }) {
  return (
    <div className="flex h-full items-center justify-center p-10 text-ink-soft">
      <p>{label} — built in a later phase.</p>
    </div>
  )
}